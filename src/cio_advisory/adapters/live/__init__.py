"""``live`` profile adapters: real research and a local model server.

Under live, house-view themes come from Gemini-grounded research over real published
market commentary (cited to their public sources), clients and portfolios are whatever
the audience registers (never the fictional samples), and narrative generation runs on
a local OpenAI-compatible model server. Everything else reuses the SDK-free local
adapters.
"""
